import java.io.IOException;
import java.util.Scanner;
public class Main {
    public static void main(String[] args) throws IOException {
    Scanner s = new Scanner(System.in);
    int km = s.nextInt();
    int min = (60 * km) / 30;
    System.out.println(String.format(min + " minutos"));
    }
}
